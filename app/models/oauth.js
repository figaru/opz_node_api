/*var mongoose = require('mongoose');
var Schema = mongoose.Schema;

// set up a mongoose model
module.exports = mongoose.model('authorizeSchema', new Schema({ 
	user: String, 
	pass: String, 
	app_id: String, 
	app_type: String, 
}));*/